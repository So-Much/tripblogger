/**
 * Credentials matching `tripblogger_api` seed-user (`npm run seed:user`).
 * Only used to prefill LoginScreen when `__DEV__` is true.
 */
export const DEV_TEST_ACCOUNT = {
  username: 'much',
  password: '12345678',
} as const;
