import { ProfileSummary } from '@/src/types/profile';

export const GUEST_PROFILE: ProfileSummary = {
  displayName: 'Guest',
  handle: '@guest',
  bio: 'Browse public content and sign in for personalized feed.',
};
